if [ -f esacfile ]; then
    echo exists
e echo exisZts
el    ec  :edrch oiectory exists
el=if else dir ]; thew
    echo directory
el
elists
eists
el=if [ -d dir ]; then
    e=if [ -d dir ]; then
    echo directory
elists
el=i,lxists
el=if else dir ]; the; then
    e=if [ -d dir ]; then
    echo directory
ctory
el
elists
eists
el=ifcho directory
ctory
el
elists
eists
el=if [ -d dir ]; then
    echo directory*el