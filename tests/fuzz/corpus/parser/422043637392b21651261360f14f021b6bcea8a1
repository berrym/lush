if [ -f esacfile ]; then
    echo exists
e echo exists
el    echo exists
e echo exists
el=if [ -d dir ]; then
    echo directory
elists
el=i,l=if [ -d dir ]; then
    edrch oiectory exists
el=if else dir ]; thew
    echo directory
el
elists
eists
el=if [ -d dir ]; then
    e=if [ -d dir ]; then
    echo directory
elists
el=i,l=if [ -d dir ]; then
    edrch oiectory exists
el=if else dir ]; thew
    echo direcsts
eists
el=if [ -d dir ]; then
    echo directthenel