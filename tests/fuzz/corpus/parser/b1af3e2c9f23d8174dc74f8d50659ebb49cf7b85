if [ -f esacfile ]; then
    echo exists
e l=<(if [ -d dir ]; then
    echo directoren
    echo existists
el=if [ -d dir ]; then
    echo directory
elistts
el=if [ -d dir ]; then
    echo directory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directoryectory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
 exists
el=if [ -d dir ]; then
    echo dil
elhsts
el=if eaA${[v 