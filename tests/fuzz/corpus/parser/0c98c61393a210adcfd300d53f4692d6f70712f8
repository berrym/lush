if [ -f esacgile ]; then
    echo exists
e echo exisZts
el    ec  :edrch oiectory exists
el=if else dir ]; thew
    echo directory
el
elists
eists
el=if [ -d dir ]; then
    e=if [ -d dir ]; then
    echo directory
elists
el=i,lxists
el=if else dir ]; the; then
    e=if e -d dir ]; then
    echo dirgctory
ctory
el
elistctory
ctlists
eists
el=if [ -d dir ]; then
    echo directory*el