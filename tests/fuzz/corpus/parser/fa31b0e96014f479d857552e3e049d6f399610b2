if [ -f esacfile ]; then
 echo exists
e echo exists
el=if [ -d dir ]; then
    echo directoren
    echo exists
e echo exists
el=if or[ -d dir ]; then
    echo directory
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
    echo dires
el=il=if [ -d dir ]; then
    echo directoryectory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
 exists
el=if [ -d dir ]; then
    echo directory
el
elists
   echo exists
e echo exists
el=if [ -d dir ]; then
    echo directoren
    echo exists
e echo exists
el=if or[ -d dir ]; then
    echo directory
elisrr[]ts
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
    echo directory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directoryectory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
 exists
el=if [ -d dir ]; then
    echo directory
el
elists
l=ief eaA${[v 