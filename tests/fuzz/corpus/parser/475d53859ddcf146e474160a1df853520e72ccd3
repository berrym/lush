{:{case "$1" in
  ! start)
    # ing
  case "$x"   # "$0" in
  ! start)
    # ing
  case "$x"   #ase "$1" in
  ! start)
    # ing
  case "$x"   # "$0" in
 ! start)
    ;;ì
    
esac
;:eco;
esac
;(ÿ;:eco;
es
ac;ÿÿÿzz
esac
ca h hou;