case "$1" in
   start)
        ec      ;;
    stop)
        g
  case "$x" in
    a) ;echo stopping
  case "$x" in
    a);; 
    b) ech;;
esac
;;
n
   start)
        ec      ;;
    stop)
        g
  case "$x" in
    a) ;gcho stopping
  case "$x" in
    a);; 
    b) echo b ;;
esa)c
;;
esesac
