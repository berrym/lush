{:{case "$1" in
  ! start)
    # ing
  case "$x"   # "$0" in
  ! start)
    # ing
  case "$x"   #ase "$1" in
  ! start)
    # ing
  case "$x"   # "$0" in
  ! start)
    # ing
  case "$x"   # ing
;(ÿ;:eco;
esac
;ÿÿÿzz
esac
ca h hou;