while e; do
    e_hmton""
od e;  while e; do
    e_hmton"e"
dod e;while e; do
  e_hi$on" le"
done < doe_hi$on" le"
done < done <n < donne <  e_hi$on" le"
done < doe_hi$on" le < done <n < donne < file
