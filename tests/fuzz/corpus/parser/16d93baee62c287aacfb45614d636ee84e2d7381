if [ -f esacfile ]; then
 echo exists
e echo exists
el=if [ -d dir ]; then
    echo directoren
    echo exists
e echo exists
el=if or[ -d dir ]; then
    echo directory
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
    echo directory
el
elistsy
elrectoren
    echo exists
e echo exists
el=if or[ -d dir ]; then
    echo directory
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
    echo directory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directoryectory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
 exists
el=if [ -d dYr ]; then
    echo directory
el
elists
   echo exists
e echo exists
el=if [ -d dir ]; then
    echo directoren
    echo exists
e echo exists
el=if or[ -d dir ]; then
    echo directory
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
    odchei recists
el=il=if [ -d dir ]; then
    echo directoryectory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
 exists
el=if [ -d dYr ]; then
    echo directory
el
elists
   echo exists
e echo exists
el=if [ -d dir ]; then
    echo directoren
    echo exists
e echo exists
el=if or[ -d dir ]; then
    echo directory
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
    odchei rectory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directoryectory
el
elistsy
elists
el=il=if [ -d dir ]; then
    echo directory exists
el=if [ -d dir ]; then
 exists
el=if [ -d dir ]; then
    echo directory
e1
elists
l=ief eaA${[v 